# 2026-09-20 完整工作区：交付差异与接续

交付 ID：`2026-09-20_完整工作区`。当前包：`产品研发与测试_AI统一工作区_展开整理版_2026-09-20.zip`。

## 比较的到底是哪两份

本次比较的是 **上一交付包 P（2026-09-19）→ 本次展开内容 N（2026-09-20）**。上一包 SHA-256：`3388c20266223de9ea541ba7a13cb429cf802f5023e393930be9fba14d110981`。已读取并检验上一 ZIP，而非依靠聊天记忆写差异。

尚未取得 GitHub 当前分支及对应提交快照，本资料不能称为 GitHub 基线 B→N。新包也不保证已吸收远程或工作电脑新增的产品事实。不得把它直接覆盖本机工作区。

- [基线与范围](基线与范围.json)：来源、范围、逐文件身份和实际 Git 命令。
- [变更清单](变更清单.csv)：新增、修改、缺失状态及二进制摘要。
- [文本差异](文本差异.diff)：Git 从实际文本生成的逐行比较，仅作审阅参考。

范围内共 296 个上一快照文件、333 个本次快照文件。变更统计：{"修改": 39, "新增": 37, "元数据重新生成（不纳入递归文本比较）": 2}。二进制不伪造逐行差异；缺失不能自动解释为删除。为避免自引用，差异资料自身以及两份生成的文件清单/核验报告不参加递归文本比较；后两项已在 CSV 单列，最终 ZIP 的内容核验另外执行。

## 本次重点变更

1. 新增真实场景集成验证，把既有产品/EMAP 事实、XML、POU 测试、设备与内部事件采集及回放串起来，提供单一执行入口。
2. 新增 BEH 两个用户观察的集中记录，修正 GUI 当前显示、历史操作数、引擎行为和业务正确性之间的证据边界。
3. 编译数据库改为逐仓逐配置核实有无和适用性，优先复用有效文件；不再使用全部没有的旧前提。
4. 承接用户反馈的双仓首轮完成、POU 支持，修正旧 FB 与 EMAP 限制，同步导航、模板和阶段汇报。
5. 纳入此前完整包之后形成的阶段汇报、配图和相关文档。详细范围以实际清单为准，不仅包含最后一轮修改。

重点三方核对：工具状态报告、产品多核背景、任务记录和事实报告、本机环境路径、Git 接入状态及文件清单。这里的模板和未核实状态不能覆盖本机已经有证据的成果；本地资料也须核对版本及适用性。文本自动合并成功不等于产品事实一致。

## 两台电脑分别怎么接续

第一台电脑的本地 Codex：把 ZIP 解到独立暂存目录，读取现有仓库、分支、远程和所有本地改动，先备份。核对 P 与 Git 历史的关系，按 P→N 的实际维护变化与当前仓库做比较；保持已有远程历史，保留尚未回传到此工作区的事实。按用户授权合并、检查和推送，返回仓库、分支、推送后的完整 commit SHA、ZIP 摘要及未完成项。不得仅因 N 缺少某个远程文件就删除它。

工作电脑 AI：先记录并备份本地提交、暂存/未暂存、未跟踪及需要保留的忽略文件；仅备份分支或一次 git diff 不覆盖全部本地状态。先 fetch，核实本次发布 SHA；从历史确定共同比较基线，分别列出上游变化和本地完整变化。保留本地独有成果，逐项处理双方修改、删除/修改、移动/修改、新增同名及语义冲突。可确定的继续合并，需要取舍的保留双方证据并集中说明。本次工作电脑接续不自动 push。

不得使用硬重置、清理未跟踪文件或整目录覆盖来完成同步。完成后检查入口、引用、适用的已有校验和最终 Git 状态，报告本地成果落点、未解决项、备份与恢复方法；fetch 成功不能写成全部合并完成。完整规则见[归档与同步规则](../../归档与同步规则.md)。

## 包含和排除

正式资料、代码/辅助脚本、配图、必要实测证据包与历史来源包一起交付。旧整包和重复专项包不套入本 ZIP；阶段汇报旧图文 ZIP 也不重复纳入，提供最新展开文档、HTML 和配图。3 个原始空日志从上一 ZIP 按原字节恢复，未重新运行其历史任务。

本次只验证文档与交付完整性，未执行公司产品测试、上板、公司工具实测或 GitHub 推送。过去的 PASS 仍只代表其原来记录的范围。下载后打开根目录 README 开始。
